[build]
  command = "npm ci && npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "18"

# Both partygemai.com and www.partygemai.com will work
# Add both domains in Netlify dashboard: Site settings → Domain management
# No redirect needed - both domains will serve the same content

